import LoadingSpinner from "@/components/LoadingSpinner";

const HomePage = () => {
    return (
      <LoadingSpinner>
        WORK IN PROGRESS!!!
      </LoadingSpinner>
    );
}

export default HomePage;